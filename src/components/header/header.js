import './header.scss';
import Menu from "../menu/menu";
const Header = () => {
    return (
        <header className="header">
            <Menu color="white" />
        </header>
    )
}

export default Header;